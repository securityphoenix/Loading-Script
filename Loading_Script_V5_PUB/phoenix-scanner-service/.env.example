# Phoenix Scanner Service - Environment Variables Example
# Copy this file to .env and update with your values

# ============================================================================
# Phoenix Platform Credentials (REQUIRED for actual imports)
# ============================================================================
PHOENIX_CLIENT_ID=your-phoenix-client-id-here
PHOENIX_CLIENT_SECRET=your-phoenix-client-secret-here
PHOENIX_API_URL=https://api.demo.appsecphx.io

# ============================================================================
# API Security (REQUIRED)
# ============================================================================
# API key(s) for client authentication (comma-separated for multiple keys)
API_KEY=changeme-insecure-key

# Secret key for session management
SECRET_KEY=changeme-secret-key

# Enable/disable authentication
ENABLE_AUTH=true

# ============================================================================
# Optional Configuration
# ============================================================================

# Logging
LOG_LEVEL=INFO
DEBUG_MODE=false

# Worker Settings
MAX_CONCURRENT_JOBS=5
JOB_TIMEOUT=3600

# File Upload
MAX_UPLOAD_SIZE_MB=500

# ============================================================================
# Required when uploads use --tv-tags; not needed otherwise.
# ============================================================================
# WORKSPACE_PREFIX=your-phoenix-tag-namespace-uuid

# ============================================================================
# Advanced Configuration (Usually don't need to change)
# ============================================================================

# API Configuration
# API_HOST=0.0.0.0
# API_PORT=8000
# API_WORKERS=4

# Redis Configuration
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_DB=0

# Database Configuration
# DATABASE_URL=sqlite:////app/data/jobs.db
# For PostgreSQL:
# DATABASE_URL=postgresql://phoenix:phoenix_secure_password@postgres:5432/phoenix_scanner

# Storage Paths
# UPLOAD_DIR=/app/uploads
# LOG_DIR=/app/logs

# Phoenix Scanner Configuration
# PHOENIX_CONFIG_FILE=/parent/config_multi_scanner.ini

# ============================================================================
# Usage Examples
# ============================================================================

# Development:
#   cp .env.example .env
#   nano .env  # Edit with your values
#   docker-compose up -d

# Production:
#   Use secrets management (Kubernetes secrets, Docker secrets, etc.)
#   or environment-specific .env files

# CI/CD:
#   Set via pipeline secrets/variables
#   export PHOENIX_CLIENT_ID=xxx
#   export PHOENIX_CLIENT_SECRET=xxx

# ============================================================================
# Security Notes
# ============================================================================

# 1. NEVER commit .env files to version control
# 2. Use different credentials per environment (dev/staging/prod)
# 3. Rotate API keys regularly
# 4. Use secrets management in production
# 5. Keep PHOENIX_CLIENT_SECRET secure

# ============================================================================
# Troubleshooting
# ============================================================================

# Check if variables are loaded:
#   docker-compose exec api env | grep PHOENIX

# Test Phoenix API connection:
#   curl -u "$PHOENIX_CLIENT_ID:$PHOENIX_CLIENT_SECRET" \
#     https://api.demo.appsecphx.io/v1/auth/access_token

# View logs:
#   docker-compose logs -f worker

